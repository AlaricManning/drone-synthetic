# local data staging — raw runs, generated datasets, QC output live in S3, not git
data/

# python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/

# terraform (the lockfile .terraform.lock.hcl IS committed — it pins providers)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# secrets — AWS credentials never live in this repo
.env
