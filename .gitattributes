# Shell scripts must keep LF endings. They are authored on Windows but run
# under WSL and in the Linux container, where a CRLF shebang line or a
# trailing \r on `set -euo pipefail` fails in ways that read as nonsense.
*.sh text eol=lf
